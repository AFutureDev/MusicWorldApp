const AlbumType = {
    id: String,
    name: String,
    by: String,
    numberOfLikes: Number,
    imageUri: String,
    artistsHeadline: String
}

const SongType = {
    id: String,
    imageUri: String,
    title: String,
    artist: String,
}