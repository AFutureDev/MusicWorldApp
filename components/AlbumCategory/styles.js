import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        margin: 10,
    },
    title: {
        color: '#000',
        fontSize: 25,
        fontWeight: 'bold',
        margin: 10
    }
})

export default styles;